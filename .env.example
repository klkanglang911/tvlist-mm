# 环境变量模板
# 复制此文件为 .env.local 并填写实际值

# 管理员密码
ADMIN_PASSWORD=your_password_here

# 数据库配置（可选）
# 默认：开发环境使用 ./data/tvlist.db，生产环境（Docker）使用 /data/tvlist.db
# DATABASE_PATH=./data/tvlist.db

# TV.TXT 访问保护（v1.2.0 新增）
# 主访问密钥（必填，建议使用随机字符串）
TV_TXT_ACCESS_KEY=your-secret-key-here

# 额外的访问密钥（可选，用于分享给朋友，逗号分隔）
# 例如：friend-key-1,friend-key-2,friend-key-3
TV_TXT_SECONDARY_KEYS=

# 速率限制（可选，默认 60 次/小时）
# 每个 IP 地址每小时允许的最大请求次数
TV_TXT_RATE_LIMIT=60
