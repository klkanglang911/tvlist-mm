# 电视直播源管理系统 (tvlist-mm)

一个专业的 Web 管理界面，用于管理电视直播源列表，支持分类管理、批量导入导出、访问密钥保护等功能。

## 功能特性

- ✅ **频道管理**：增删改查频道，支持搜索和分类筛选
- ✅ **分类管理**：自定义频道分类，灵活组织
- ✅ **批���导入**：支持 TXT、M3U 等多种格式
- ✅ **多格式导出**：导出为 TXT 或 M3U 格式
- ✅ **密码保护**：管理界面需要密码访问
- ✅ **访问密钥**：支持多个访问密钥和速率限制
- ✅ **公开访问**：生成受保护的 TV 列表 URL 供 VLC 等播放器使用
- ✅ **Docker 部署**：支持 Docker 一键部署到 VPS

## 技术栈

- **前端**: Next.js 15 (App Router) + React 19 + TypeScript
- **样式**: Tailwind CSS
- **数据存储**: SQLite (本地数据库)
- **部署方式**:
  - Docker + VPS（推荐，独立部署）
  - Vercel（Serverless 部署，需配置外部数据库）

## 部署方式

### 方式一：Docker 部署到 VPS（推荐）

适用于拥有 VPS 服务器的用户，提供完整的数据控制和隐私保护。

#### 1. 准备工作

**系统要求**：
- VPS 配置：2核2G 或以上
- 操作系统：Linux (Ubuntu/Debian/CentOS)
- 已安装：Docker + Docker Compose

**安装 Docker**（如未安装）：
```bash
# Ubuntu/Debian
curl -fsSL https://get.docker.com | sh
sudo usermod -aG docker $USER

# 安装 Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose
```

#### 2. 克隆项目

```bash
cd /opt
git clone https://github.com/klkanglang911/tvlist-mm.git
cd tvlist-mm
```

#### 3. 配置环境变量

```bash
# 复制环境变量模板
cp .env.docker.example .env

# 编辑配置文件
nano .env
```

填写以下配置：

```bash
# ========================================
# 管理员配置
# ========================================
ADMIN_PASSWORD=your_secure_password

# ========================================
# HTTPS 配置
# ========================================
# 如果使用 HTTPS 访问（如通过 Nginx SSL），设置为 true
# 如果使用 HTTP 访问（如 http://IP:端口），设置为 false
ENABLE_HTTPS=false

# ========================================
# 数据库配置
# ========================================
DATABASE_PATH=/data/tvlist.db

# ========================================
# TV.TXT 访问保护
# ========================================
TV_TXT_ACCESS_KEY=your-secret-key-here
TV_TXT_SECONDARY_KEYS=
TV_TXT_RATE_LIMIT=60
```

**重要说明**：
- `ADMIN_PASSWORD`：管理后台登录密码，建议使用强密码
- `ENABLE_HTTPS`：HTTP 访问设为 `false`，HTTPS 访问设为 `true`
- `TV_TXT_ACCESS_KEY`：访问直播源列表的密钥
- `TV_TXT_RATE_LIMIT`：每小时最大访问次数（默认 60）

#### 4. 修改端口（可选）

如果 3000 端口已被占用，编辑 `docker-compose.yml`：

```bash
nano docker-compose.yml
```

找到 `ports` 部分，修改为其他端口：
```yaml
ports:
  - "8080:3000"  # 将 8080 改为你想要的端口
```

#### 5. 部署

```bash
# 一键部署脚本
./docker-deploy.sh

# 或手动部署
docker-compose up -d --build
```

#### 6. 验证部署

```bash
# 查看容器状态
docker-compose ps

# 查看日志
docker logs tvlist-mm

# 访问应用
# 浏览器打开: http://你的VPS_IP:3000/login
```

#### 7. 配置域名和 SSL（可选）

如果使用 1Panel 或 Nginx：

**使用 1Panel**：
1. 进入 1Panel → 网站 → 创建网站
2. 类型：反向代理
3. 代理地址：`http://127.0.0.1:3000`
4. 配置 SSL 证书（Let's Encrypt）

**使用 Nginx**：
```nginx
server {
    listen 80;
    server_name tv.example.com;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

配置 SSL 后，记得将 `.env` 中的 `ENABLE_HTTPS` 改为 `true`。

### 方式二：Vercel 部署（需配置外部数据库）

⚠️ 注意：v1.3.0+ 版本使用 SQLite，Vercel 不支持本地文件存储。如需部署到 Vercel，需要配置外部数据库（如 Turso、PlanetScale）。

详细步骤请参考 `DEPLOYMENT.md`。

## 使用指南

### 登录管理后台

访问 `http://你的域名或IP:端口/login`，输入 `ADMIN_PASSWORD` 登录。

### 添加分类

1. 进入"分类管理"
2. 点击"添加分类"
3. 输入分类名称（如"央视频道"、"卫视频道"）
4. 保存

### 添加频道

1. 进入"频道管理"
2. 点击"添加频道"
3. 填写：
   - 频道名称（如"CCTV-1"）
   - 直播源 URL（如 `http://example.com/cctv1.m3u8`）
   - 选择分类
4. 保存

### 批量导入

1. 进入"导入导出"
2. 选择导入模式：
   - **追加模式**：保留现有频道，添加新频道
   - **替换模式**：清空现有频道，导入新频道
3. 粘贴频道列表
4. 点击"开始导入"

**支持的格式**：

```text
# 格式 1：逗号分隔
CCTV-1,http://example.com/cctv1.m3u8
CCTV-2,http://example.com/cctv2.m3u8

# 格式 2：空格分隔
CCTV-1 http://example.com/cctv1.m3u8
CCTV-2 http://example.com/cctv2.m3u8

# 格式 3：M3U 格式
#EXTINF:-1,CCTV-1
http://example.com/cctv1.m3u8
#EXTINF:-1,CCTV-2
http://example.com/cctv2.m3u8

# 支持分类标记
央视频道,#genre#
CCTV-1,http://example.com/cctv1.m3u8
```

### 导出频道列表

1. 进入"导入导出"
2. 选择导出格式（TXT 或 M3U）
3. 点击下载

### 访问密钥管理

1. 进入"密钥管理"
2. 查看默认密钥或添加新密钥
3. 每个密钥可以设置标签（方便管理）
4. 查看密钥最后使用时间

### 在播放器中使用

**公开访问地址**：
```
http://你的域名或IP:端口/tv.txt?key=你的访问密钥
```

**VLC 播放器**：
1. 打开 VLC
2. 媒体 → 打开网络串流
3. 输入上述地址
4. 播放

**电视盒子/IPTV 应用**：
在直播源设置中填入上述地址即可。

## Docker 管理命令

```bash
# 查看容器状态
docker-compose ps

# 查看日志
docker logs tvlist-mm
docker logs tvlist-mm --tail 50
docker logs tvlist-mm -f  # 实时日志

# 重启容器
docker-compose restart

# 停止容器
docker-compose stop

# 启动容器
docker-compose start

# 停止并删除容器
docker-compose down

# 重新构建并启动
docker-compose up -d --build

# 进入容器内部
docker exec -it tvlist-mm sh

# 备份数据库
docker cp tvlist-mm:/data/tvlist.db ./backup-$(date +%Y%m%d).db

# 恢复数据库
docker cp ./backup.db tvlist-mm:/data/tvlist.db
docker-compose restart
```

## 数据管理

### 数据存储位置

Docker 部署时，数据存储在 Docker Volume `tvlist-data` 中：
```
/var/lib/docker/volumes/tvlist-data/_data/tvlist.db
```

### 备份数据

```bash
# 方式一：直接复制数据库文件
docker cp tvlist-mm:/data/tvlist.db ./tvlist-backup-$(date +%Y%m%d).db

# 方式二：备份整个 Volume
docker run --rm -v tvlist-data:/data -v $(pwd):/backup ubuntu tar czf /backup/tvlist-data-$(date +%Y%m%d).tar.gz /data
```

### 恢复数据

```bash
# 停止容器
docker-compose stop

# 恢复数据库文件
docker cp ./tvlist-backup.db tvlist-mm:/data/tvlist.db

# 启动容器
docker-compose start
```

### 迁移数据到新服务器

```bash
# 在旧服务器上备份
docker cp tvlist-mm:/data/tvlist.db ./tvlist.db

# 将 tvlist.db 传输到新服务器

# 在新服务器上部署后恢复
docker cp ./tvlist.db tvlist-mm:/data/tvlist.db
docker-compose restart
```

## 更新应用

```bash
cd /opt/tvlist-mm

# 拉取最新代码
git pull

# 重新构建并启动
docker-compose down
docker-compose build --no-cache
docker-compose up -d

# 查看日志确认启动成功
docker logs tvlist-mm
```

## 常见问题

### 1. 无法登录，提示"加载中"后又回到登录页

**原因**：Cookie Secure 标志问题。

**解决方法**：
- 检查 `.env` 中的 `ENABLE_HTTPS` 配置
- HTTP 访问时设为 `false`
- HTTPS 访问时设为 `true`
- 修改后重启容器：`docker-compose restart`

### 2. 添加分类/频道失败，提示"数据库错误"

**原因**：数据库目录权限问题。

**解决方法**：
```bash
# 查看容器日志
docker logs tvlist-mm

# 如果看到权限错误，重新部署
docker-compose down
docker volume rm tvlist-mm_tvlist-data
docker-compose up -d
```

### 3. 端口 3000 已被占用

**解决方法**：
编辑 `docker-compose.yml`，修改端口映射：
```yaml
ports:
  - "8080:3000"  # 改为其他端口
```
然后重新部署：`docker-compose up -d`

### 4. VPS 磁盘空间不足

**解决方法**：
```bash
# 清理 Docker 缓存和未使用的镜像
docker system prune -a --volumes -f

# 查看释放的空间
df -h
```

### 5. 如何修改管理员密码

1. 编辑 `.env` 文件，修改 `ADMIN_PASSWORD`
2. 重启容器：`docker-compose restart`

### 6. 如何修改访问密钥

两种方式：
- **方式一**：在管理后台的"密钥管理"中添加/删除密钥
- **方式二**：修改 `.env` 文件的 `TV_TXT_ACCESS_KEY`，重启容器

### 7. 数据会丢失吗？

不会。数据存储在 Docker Volume 中，即使容器重启或重新创建，数据依然保留。但建议定期备份数据库文件。

## 项目结构

```
tvlist-mm/
├── app/
│   ├── api/                   # API 路由
│   │   ├── auth/             # 认证相关
│   │   ├── channels/         # 频道管理
│   │   ├── categories/       # 分类管理
│   │   ├── import/           # 导入
│   │   ├── export/           # 导出
│   │   └── access-keys/      # 访问密钥管理
│   ├── dashboard/            # 管理后台页面
│   ├── login/                # 登录页
│   └── tv.txt/               # 公开访问的频道列表
├── lib/                      # 工具函数
│   ├── database.ts           # SQLite 数据库
│   ├── auth.ts               # 认证逻辑
│   ├── data.ts               # 数据操作
│   └── parser.ts             # 文件解析
├── types/                    # TypeScript 类型定义
├── Dockerfile                # Docker 镜像构建文件
├── docker-compose.yml        # Docker Compose 配置
├── docker-entrypoint.sh      # 容器启动脚本
└── package.json
```

## 开发

```bash
# 安装依赖
npm install

# 开发模式
npm run dev

# 构建
npm run build

# 生产模式运行
npm start

# 代码检查
npm run lint
```

## 版本历史

- **v1.5.0** (2025-11-17)
  - ✨ 新增导入预览和测试功能
  - ✨ 导入前可预览所有频道并测试在线状态
  - ✨ 支持选择性导入在线频道
  - ✨ 实时显示频道测试进度和结果
  - ✨ 新增离线频道列表对话框（点击离线统计数字查看）
  - 🎨 优化导入流程用户体验
  - 📊 导入预览界面显示频道详细信息和测试状态

- **v1.4.0** (2025-11-17)
  - ✨ 新增频道状态检测功能
  - ✨ 支持手动测试所有频道（在线/离线状态）
  - ✨ 显示响应时间和最后检测时间
  - ✨ 新增定时任务功能（每日自动检测）
  - ✨ 支持 Webhook 通知（企业微信、钉钉、飞书、自定义）
  - ✨ 新增系统设置页面（图形化配置）
  - 📊 实时测试进度显示
  - 📊 频道状态统计卡片（在线率、在线数、离线数）

- **v1.3.0** (2025-11-17)
  - 🚀 迁移到 SQLite 本��数据库
  - 🐛 修复 HTTP 环境下的 Cookie 问题
  - 🐛 修复 Docker Volume 权限问题
  - 📝 完善 Docker 部��文档
  - ✨ 支持独立 VPS 部署

- **v1.1.0** - 添加访问密钥管理功能
- **v1.0.0** - 初始版本，基于 GitHub 数据存储

## 技术支持

如有问题，请在 GitHub Issues 中提交：
https://github.com/klkanglang911/tvlist-mm/issues

## 许可

MIT License
