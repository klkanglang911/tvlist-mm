# Docker 部署环境变量配置
# 复制此文件为 .env 并填写实际值

# ========================================
# 管理员配置
# ========================================
ADMIN_PASSWORD=your_password_here

# ========================================
# HTTPS 配置
# ========================================
# 是否启用 HTTPS（true/false）
# 如果使用 HTTPS 访问（如通过 Nginx SSL），设置为 true
# 如果使用 HTTP 访问（如 http://IP:端口），设置为 false
ENABLE_HTTPS=false

# ========================================
# 数据库配置
# ========================================
# Docker 环境使用 Volume 挂载的路径
DATABASE_PATH=/data/tvlist.db

# ========================================
# TV.TXT 访问保护
# ========================================
# 主访问密钥（必填）
TV_TXT_ACCESS_KEY=your-secret-key-here

# 额外的访问密钥（可选，逗号分隔）
TV_TXT_SECONDARY_KEYS=

# 速率限制（可选，默认 60 次/小时）
TV_TXT_RATE_LIMIT=60

# ========================================
# 日志配置
# ========================================
# 日志级别：debug | info | warn | error | none
# 生产环境建议使用 warn 或 error 以减少日志量
# 如遇问题需要调试，可临时改为 debug
LOG_LEVEL=warn
